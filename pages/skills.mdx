---
type: page
title: Skills
date: 2021-03-17
---
import Image from 'next/image'
import skills from '../utils/skills.json'

## Main Hard Skills


<div className='hard-skills'>
  {
    skills.hardSkills.map(skill => (
      <div className='hard-skills-container' key={skill}>
        <Image
          className='hard-skills-img'
          src={`/images/skills/${skill}.svg`}
          width={75}
          height={50}
          alt={skill}
        />
      </div>
    ))
  }
</div>

## Main Soft Skills

<div className='soft-skills'>
  <div className='soft-skills-card'>
    <ul>
      {
        skills.softSkills.slice(0,5).map(skill => (
          <li key={skill}>
            {skill}
          </li>   
      ))
      }
    </ul>
  </div>
  <div className='soft-skills-card'>
    <ul>
      {
        skills.softSkills.slice(5,10).map(skill => (
          <li key={skill}>
            {skill}
          </li>   
      ))
      }
    </ul>
  </div>
</div>